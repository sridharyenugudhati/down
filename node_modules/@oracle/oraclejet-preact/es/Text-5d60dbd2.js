/* @oracle/oraclejet-preact: 15.1.5 */
import { jsx } from 'preact/jsx-runtime';
import './utils/UNSAFE_classNames.js';
import { mergeInterpolations } from './utils/UNSAFE_mergeInterpolations.js';
import { styles, multiVariantStyles } from './UNSAFE_Text/themes/TextStyles.css.js';
import { c as classNames } from './classNames-6df97f98.js';

const interpolations = [
    ({ lineClamp }) => !lineClamp ? {} : { class: styles.lineClamp, webkitLineClamp: lineClamp }
];
const styleInterpolations = mergeInterpolations(interpolations);
function Text({ children, hyphens, size, truncation, variant, weight, ...props }) {
    const variantClasses = multiVariantStyles({
        hyphens,
        size,
        truncation,
        variant,
        weight
    });
    const { class: interpolatedClasses, ...styles } = styleInterpolations(props);
    const classes = classNames([variantClasses, interpolatedClasses]);
    return (jsx("span", { class: classes, style: styles, id: props.id, children: children }));
}

export { Text as T };
//# sourceMappingURL=Text-5d60dbd2.js.map
