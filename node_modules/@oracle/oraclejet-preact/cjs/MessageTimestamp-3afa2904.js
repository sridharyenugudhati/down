/* @oracle/oraclejet-preact: 15.1.5 */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
require('./utils/UNSAFE_classNames.js');
var PRIVATE_Message_themes_MessageStyles_css = require('./PRIVATE_Message/themes/MessageStyles.css.js');
var MessageFormattingUtils = require('./MessageFormattingUtils-5e7b02d0.js');
var classNames = require('./classNames-9be98b10.js');

/**
 * Timestamp Component for rendering timestamp in Message
 */
function MessageTimestamp({ value, variant = 'banner' }) {
    // If detail content is rendered, then wrap it in a div with specified style classes
    // TODO: JET-48715 - remove marker classes
    // Right now, oj-message-banner component uses this for unit tests and has no alternate way for
    // testing. This will need to be removed as we work on oj-c-message-banner.
    // TODO:  Refactor to utilize useComponentTheme and multiVariantStyles.
    const markerClassForTests = 'oj-c-messagebanner-timestamp';
    const classes = classNames.classNames([
        variant === 'banner' && markerClassForTests,
        PRIVATE_Message_themes_MessageStyles_css.messageTimestampStyles[variant]
    ]);
    // Otherwise, render the timestamp
    const formattedTimestamp = MessageFormattingUtils.formatTimestamp(value);
    return jsxRuntime.jsx("div", { class: classes, children: formattedTimestamp });
}

exports.MessageTimestamp = MessageTimestamp;
//# sourceMappingURL=MessageTimestamp-3afa2904.js.map
