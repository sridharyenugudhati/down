#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../@oracle/ojet-cli/bin/ojet" "$@"
else 
  exec node  "$basedir/../@oracle/ojet-cli/bin/ojet" "$@"
fi
